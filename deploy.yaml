kind: Deployment
apiVersion: apps/v1
metadata:
  name: flight-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flight-java-app
  template:
    metadata:
      labels:
        app: flight-java-app
    spec:
      containers:
        - name: flight-java
          image: flight-java-image:latest
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          resources:
            requests:
              memory: "180Mi"
              cpu: "10m"
---
kind: Service
apiVersion: v1
metadata:
  name: flight-java-svc
spec:
  ports:
    - name: http-8081
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: flight-java-app